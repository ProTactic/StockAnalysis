   /*** company overview tab **/
    /*private JTabbedPane tabbedPane1;
    private JLabel CompanyName;
    private JLabel CompanyCountry;
    private JLabel CompanyCurrency;
    private JLabel CompanySector;*/

    public MainWindow(){
        super();

        try {
            systemController = SystemController.getInstance();
        } catch (StockSystemException e) {}

        initializeUI();
        setActions();

    }

    private void initializeUI(){
        this.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        this.setSize(720,640);
        this.setContentPane(mainPanel);

        this.pack();
        this.setVisible(true);
        this.toFront();
    }

    private void setActions(){
        SearchButton.addActionListener(new ButtonSearchAction());
    }

    protected class ButtonSearchAction implements ActionListener{
        @Override
        public void actionPerformed(ActionEvent e) {
            String symbol = SearchTextField.getText();
            CompanyOverviewDTO companyOverview = systemController.getCompanyOverview(symbol);
            /*CompanyName.setText(String.format("%s (%s : %s)", companyOverview.name,
                                                companyOverview.exchange, symbol.toUpperCase()));
            CompanyCountry.setText("Country : " + companyOverview.country);
            CompanyCurrency.setText("Currency : " + companyOverview.currency);
            CompanySector.setText("Sector : " + companyOverview.sector);*/
        }
    }